{
  "name": "slack-links",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "tape 'tests/**/*.js' | tap-diff",
    "reset": "cat reset.sql | psql && cat schema.sql | psql slack_links"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "request": "^2.69.0"
  },
  "devDependencies": {
    "tap-diff": "^0.1.1",
    "tape": "^4.4.0"
  }
}
